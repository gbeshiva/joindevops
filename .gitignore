###########################################
# TERRAFORM GLOBAL IGNORE RULES
###########################################

# Ignore all .terraform directories everywhere (root + all subfolders)
.terraform/
**/.terraform/

# Terraform dependency lock file
.terraform.lock.hcl

# Terraform state files (contain secrets)
*.tfstate
*.tfstate.*

# Crash logs
crash.log
crash.*.log

# Sensitive variable definitions (NEVER commit)
*.tfvars
*.tfvars.json

# Local override files (not for shared repos)
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Terraform apply lock info file
.terraform.tfstate.lock.info

# Allow override files only if explicitly needed
!example_override.tf

# Ignore Terraform plan output
# (e.g. terraform plan -out=tfplan)
*tfplan*
*tfplan.*

# Terraform CLI config files
.terraformrc
terraform.rc


###########################################
# GENERAL OPTIONAL IGNORE RULES
###########################################

# macOS
.DS_Store

# Linux
*.swp

# Windows
Thumbs.db

# Logs
*.log

# Backup files
*~
*.bak
